# Service for devops-cli
# Generated by Rig CLI

apiVersion: v1
kind: Service
metadata:
  name: devops-cli
  labels:
    app: devops-cli
    generated-by: rig-cli
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: devops-cli
---