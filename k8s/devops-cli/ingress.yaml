# Ingress for devops-cli
# Generated by Rig CLI

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-cli
  labels:
    app: devops-cli
    generated-by: rig-cli
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - devops-cli.aibrandedbooth.com
    secretName: devops-cli-tls
  rules:
  - host: devops-cli.aibrandedbooth.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: devops-cli
            port:
              number: 80
---